<nav class="bg-white border-b border-gray-200" data-navbar>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-[1fr_auto_1fr] items-center h-16">
            {% set logoUrl = logo_url() %}
            <div class="col-start-1 justify-self-start">
                <a href="{{ path('app_front_home') }}" class="flex items-center">
                    {% if logoUrl %}
                        <img src="{{ logoUrl }}" alt="{{ site_name() }}" class="h-10 w-auto">
                    {% else %}
                        <span class="font-['Bodoni_Moda',serif] font-normal uppercase text-base md:text-2xl text-gray-900">{{ site_name() }}</span>
                    {% endif %}
                </a>
            </div>

            <div class="hidden lg:flex lg:col-start-2 space-x-8">
                {# Pages #}
                {% for item in nav_items() %}
                    <a href="{{ path(item.route) }}" class="text-gray-600 hover:text-gray-900 transition">
                        {{ item.label|trans }}
                    </a>
                {% endfor %}
            </div>

            {% set currentLocale = app.request.locale %}
            {% set otherLocale = currentLocale == 'fr' ? 'en' : 'fr' %}
            {% set switchLabel = otherLocale == 'en' ? 'Switch to English' : 'Passer en français' %}

            <div class="hidden lg:flex col-start-3 justify-self-end">
                <form method="post" action="{{ path('app_locale_switch', {locale: otherLocale}) }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('locale_switch') }}">
                    <button type="submit"
                            class="inline-flex items-center justify-center p-1 rounded hover:opacity-75 transition focus:outline-none cursor-pointer"
                            aria-label="{{ switchLabel }}"
                            title="{{ switchLabel }}">
                        {{ include('partials/icons/flag-' ~ otherLocale ~ '.html.twig', {class: 'w-7 h-5'}) }}
                    </button>
                </form>
            </div>

            <button type="button"
                    class="lg:hidden col-start-3 justify-self-end inline-flex items-center justify-center p-2 rounded-md text-gray-700 hover:bg-gray-100 focus:outline-none"
                    aria-label="Ouvrir le menu"
                    aria-expanded="false"
                    data-navbar-toggle>
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                </svg>
            </button>
        </div>
    </div>

    {# Menu mobile #}
    <div class="hidden lg:hidden fixed inset-0 z-50 bg-black/60" data-navbar-menu>
        <div class="absolute top-0 right-0 h-full w-full bg-white shadow-xl flex flex-col">
            <div class="flex items-center justify-between h-16 px-4 border-b border-gray-200">
                <span class="text-lg font-bold text-gray-900">Menu</span>
                <button type="button"
                        class="inline-flex items-center justify-center p-2 rounded-md text-gray-700 hover:bg-gray-100 focus:outline-none"
                        aria-label="Fermer le menu"
                        data-navbar-close
                >
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 6l12 12M6 18L18 6"/>
                    </svg>
                </button>
            </div>

            <nav class="flex-1 overflow-y-auto px-4 py-8 space-y-6">
                {% for item in nav_items() %}
                    <a href="{{ path(item.route) }}" class="block px-3 py-3 rounded text-center text-gray-600 hover:bg-gray-100 hover:text-gray-900 transition">
                        {{ item.label|trans }}
                    </a>
                    {% if not loop.last %}
                        <div class="mx-auto w-[5%] h-px bg-gray-300"></div>
                    {% endif %}
                {% endfor %}
            </nav>
            <div class="px-4 py-4 border-t border-gray-200 space-y-4">
                <div class="flex items-center justify-center">
                    <form method="post" action="{{ path('app_locale_switch', {locale: otherLocale}) }}">
                        <input type="hidden" name="_token" value="{{ csrf_token('locale_switch') }}">
                        <button type="submit"
                                class="inline-flex items-center justify-center p-1 rounded hover:opacity-75 transition focus:outline-none cursor-pointer"
                                aria-label="{{ switchLabel }}"
                                title="{{ switchLabel }}">
                            {{ include('partials/icons/flag-' ~ otherLocale ~ '.html.twig', {class: 'w-8 h-6'}) }}
                        </button>
                    </form>
                </div>

                {% set links = social_links() %}
                {% if links is not empty %}
                    <div class="flex items-center justify-center space-x-4">
                        {% for link in links %}
                            <a href="{{ link.url }}" target="_blank" rel="noopener noreferrer"
                               class="hover:opacity-75 transition" aria-label="{{ link.platform }}">
                                {{ include('partials/icons/' ~ link.platform ~ '.html.twig', {class: 'w-6 h-6', colored: true}, ignore_missing=true) }}
                            </a>
                        {% endfor %}
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
